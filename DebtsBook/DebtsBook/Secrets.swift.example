import Foundation

// Copy this file to Secrets.swift (git-ignored) and fill in your Supabase project's
// values from Settings > API in the Supabase dashboard.
enum Secrets {
    static let supabaseURL = URL(string: "https://YOUR_PROJECT_REF.supabase.co")!
    static let supabaseAnonKey = "YOUR_ANON_KEY"
}
